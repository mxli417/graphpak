# Will later hold a G(C)NN KNG embedding suitable for graph traversal or LLMs
from typing import Dict, List


def kng_triples2gems(kng_triples: List[Dict]) -> List[int]:
    raise NotImplementedError(f"Not yet part of the library, coming soon")
